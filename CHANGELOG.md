# @devscast/datazen

# Unreleased
- Functional/Statement parity pass (Datazen-reference): replaced blanket skips in `src/__tests__/functional/statement.test.ts` with runtime-gated behavior aligned to Datazen intent. Added `binds invalid named parameter` (runs only on named-binding drivers), added `fetches long BLOB values` using Node `convertToNodeValue` semantics, and replaced strict numeric assertions with normalized numeric comparisons to match cross-driver equality intent. Also gated `executes with redundant parameters` for drivers/platforms that do not report redundant bindings (named-binding + MySQL-family), and validated this file on `sqlite3`, `mysql`, `mariadb`, `postgresql`, and `sqlserver`.
- Schema parity pass (Datazen-reference): aligned `AbstractSchemaManager` list APIs with legacy schema-fetch behavior (`listTableColumns`, `listTableIndexes`, `listTableForeignKeys`) and fixed `introspectTablePrimaryKeyConstraint()` to return `null` when a table is missing. Also aligned FK/PK metadata processors to quoted-name semantics (`PrimaryKeyConstraintColumnMetadataProcessor`, `ForeignKeyConstraintColumnMetadataProcessor`), normalized comparator platform-option null-vs-undefined comparison noise, and updated affected parity tests (`schema-manager`, `foreign-key-constraint-editor`, `abstract-comparator-test-case`, `metadata-providers`, `platform-parity`, `schema-name-introspection`) to match Datazen intent.
- Functional/Schema vendor-matrix pass: replaced remaining placeholder `it.skip(...)` files with real, runtime-gated Datazen-reference tests in `src/__tests__/functional/schema/{sqlite-schema-manager,mysql-schema-manager,sql-server-schema-manager,db2-schema-manager,oracle-schema-manager}.test.ts` plus `src/__tests__/functional/schema/mysql/json-collation.test.ts` and `src/__tests__/functional/schema/oracle/comparator.test.ts`.
- Schema manager parity: aligned unsupported database-name introspection behavior with Datazen expectations for SQLite/DB2 by making `listDatabases()` explicitly throw `NotSupported` in `src/schema/sqlite-schema-manager.ts` and `src/schema/db2-schema-manager.ts` (instead of returning empty lists).
- Functional/Schema SQLite comparator parity: replaced `src/__tests__/functional/schema/sqlite/comparator.test.ts` placeholder with a Datazen-aligned `ComparatorTest::testChangeTableCollation` port (real diff/apply/re-diff flow), and fixed the underlying SQLite implementation gaps by enabling SQLite column-collation support in `src/platforms/sqlite-platform.ts`, wiring `SQLiteSchemaManager.createComparator()` to the SQLite-specific comparator in `src/schema/sqlite-schema-manager.ts`, preserving renamed-column state when cloning in `src/platforms/sqlite/comparator.ts`, and extending SQLite metadata introspection in `src/platforms/sqlite/sqlite-metadata-provider.ts` to parse column collations from table DDL (with `BINARY` default normalization compatibility).
- Functional/Schema parity: replaced the `Functional/Schema/AlterTableTest` placeholder with a Doctrine-aligned port in `src/__tests__/functional/schema/alter-table.test.ts` (primary-key migration matrix, FK replacement migration, and Doctrine-equivalent platform/runtime skips).
- SQLite alter-table parity: aligned `SQLitePlatform.getAlterTableSQL()` with Doctrine behavior by adding a real "simple add-column" gate and routing all non-simple index/foreign-key/PK alter cases through table-rebuild SQL generation (instead of generic `ALTER TABLE`/`DROP INDEX` paths that SQLite cannot execute for these diffs).
- Foreign-key comparator parity: aligned `ForeignKeyConstraint.onUpdate()/onDelete()` normalization with Doctrine semantics in `src/schema/foreign-key-constraint.ts` (`NO ACTION` and `RESTRICT` now normalize to `null` for diff comparison), which removes false-positive FK diffs after SQLite introspection/rebuild.
- Functional/Schema parity pass: replaced placeholder skips with Doctrine-aligned ports for `Functional/Schema/ForeignKeyConstraintTest`, `Functional/Schema/CustomIntrospectionTest`, and `Functional/Schema/SchemaManagerTest` in `src/__tests__/functional/schema/{foreign-key-constraint,custom-introspection,schema-manager}.test.ts` (with platform/runtime skips only where Doctrine intent requires capability gating).
- Schema/runtime parity fixes surfaced by the new ports: fixed unnamed foreign-key storage in `src/schema/table.ts` so multiple unnamed FKs are preserved (no empty-name overwrite), made schema-manager table introspection lookup on non-schema platforms tolerant of quoted/dotted/legacy-invalid identifier forms in `src/schema/abstract-schema-manager.ts`, and improved SQLite metadata introspection in `src/platforms/sqlite/sqlite-metadata-provider.ts` by inferring identity/autoincrement for single integer primary-key columns (matching Doctrine SQLite behavior expectations).
- Local functional runner: added `bun run test:functional:local` (backed by `scripts/run-functional-tests-local.mjs`) to run the full functional suite sequentially against local Docker-backed `sqlite3`, `mysql`, `mariadb`, `postgresql`, and `sqlserver` targets. The runner reuses the same JSON profiles in `ci/github/vitest/*.json` and applies the local compose MariaDB port override (`3307`) automatically.
- MySQL comparator metadata refactor (async I/O parity): `CharsetMetadataProvider` and `CollationMetadataProvider` are now async (`Promise<string | null>`), connection-backed and caching providers were converted to async/await, and the legacy sync DB query contract (`src/platforms/_internal/sync-query-connection.ts`) was removed. MySQL schema manager comparator wiring now preloads charset/collation metadata asynchronously in `initialize()` and constructs the MySQL comparator with in-memory lookup maps so `compareTables()` stays pure/synchronous while all DB access is awaited ahead of time. Also preserved explicit `renamedColumns` state when cloning tables inside the MySQL comparator and restored Doctrine comparator defaults (`ComparatorConfig` rename/index detection and modified-index reporting default to `true`).
- Functional/Platform parity (alter/rename batch): added Doctrine-based ports for `Functional/Platform/AlterColumnLengthChangeTest`, `AlterDecimalColumnTest`, `AlterColumnTest`, and `RenameColumnTest` in `src/__tests__/functional/platform/*.test.ts`, and validated the batch on real `sqlite3`, `mysql`, `mariadb`, `postgresql`, and `sqlserver` targets (with only Doctrine-intent runtime skips preserved in `AlterColumnTest` where applicable).
- Comparator parity: aligned `Schema\\Comparator` closer to Datazen/Doctrine by supporting platform-aware comparator construction from schema managers, using platform-aware `columnsEqual()` for same-name column comparison, and restoring Doctrine defaults in `ComparatorConfig` (`detectRenamedColumns`, `detectRenamedIndexes`, `reportModifiedIndexes` now default to `true`). Also fixed `AbstractPlatform.columnsEqual()` to compare declarations with a shared placeholder column name so pure renames are not treated as semantic differences.
- Schema manager parity (Node async constraint): `AbstractSchemaManager.createComparator()` / `SQLServerSchemaManager.createComparator()` now pass the active platform into the comparator; `MySQLSchemaManager.createComparator()` explicitly keeps the generic comparator for now because the MySQL-specific comparator path in this port still depends on sync metadata providers (documented in code).
- PostgreSQL metadata parity: `PostgreSQLMetadataProvider` now selects `collation_name` from `information_schema.columns`, restoring collation-aware schema comparison in `Functional/Platform/AlterColumnTest` (`supports collations` / ICU collation providers). Updated `src/__tests__/platforms/metadata-providers.test.ts` fixtures for the new query shape.
- Functional/Platform parity: added a Doctrine-based port of `Functional/Platform/AddColumnWithDefaultTest` in `src/__tests__/functional/platform/add-column-with-default.test.ts` and validated it on real `sqlite3`, `mysql`, `mariadb`, `postgresql`, and `sqlserver` targets.
- Alter-table parity (column add path): implemented generic `ADD COLUMN` SQL generation in `AbstractPlatform.getAlterTableSQL()` (while still leaving changed/dropped column alters unsupported), plus a SQL Server-specific `getAlterTableSQL()` override that emits `ALTER TABLE ... ADD <column with default>` using Doctrine-aligned SQL Server column declarations and tolerates platform-options-only comparator noise in the add-column flow.
- SQLite metadata parity: `SQLiteMetadataProvider` now parses declared column lengths from `PRAGMA table_info(...).type` (for example `VARCHAR(8)`), eliminating false-positive column diffs during schema comparison in functional alter-table scenarios such as `AddColumnWithDefaultTest`.
- Functional/Platform parity (column/other-schema batch): added Doctrine-based ports for `Functional/Platform/OtherSchemaTest` and the supported `Functional/Platform/ColumnTest/*` variants (`MySQL`, `PostgreSQL`, `SQLite`, `SQLServer`) in `src/__tests__/functional/platform/other-schema.test.ts` and `src/__tests__/functional/platform/column-test/*.test.ts`, with a shared abstract helper mirroring Doctrine’s `AbstractColumnTestCase`. Validated the new files on real `sqlite3`, `mysql`, `mariadb`, `postgresql`, and `sqlserver` targets (runtime skips preserved for non-matching platform variants and Doctrine’s MySQL/SQL Server no-length varbinary cases).
- Platform/driver parity fixes surfaced by the new functional platform tests: aligned `AbstractPlatform.getStringTypeDeclarationSQL()` with Datazen/Doctrine by honoring `fixed` (`CHAR` vs `VARCHAR`) and aligned `getBinaryTypeDeclarationSQLSnippet()` to emit bare `BINARY` when length is omitted; aligned `SQLServerPlatform` string declarations with Doctrine (`NCHAR`/`NVARCHAR` by default, `ASCII` -> `CHAR`/`VARCHAR`) including `ColumnLengthRequired` behavior for `NVARCHAR` without length; aligned `SQLitePlatform.getCreateIndexSQL()` with Doctrine for schema-qualified SQLite tables (attached databases) by prefixing the schema on the index name and using the bare table name in `ON ...`; and restored SQLite default-expression parity by using `CURRENT_DATE`/`CURRENT_TIME`.
- SQLite DriverManager parity: `SQLite3Driver.connect()` now supports Doctrine-style path-based sqlite params (`path`, string `database`, `memory`) by opening a native `sqlite3.Database` asynchronously (while still preferring provided native `database`/`connection`/`client` objects). This enables `DsnParser` + `DriverManager` file-backed SQLite flows used by `Functional/Platform/OtherSchemaTest`.
- Functional/Platform parity (expression batch): added Doctrine-based ports for `Functional/Platform/BitwiseExpressionTest`, `ConcatExpressionTest`, `DateExpressionTest`, `LengthExpressionTest`, and `ModExpressionTest` in `src/__tests__/functional/platform/*.test.ts`. Validated on real `sqlite3`, `mysql`, `mariadb`, `postgresql`, and `sqlserver` targets. Kept one runtime skip for SQL Server 4-byte UTF-8 length behavior when the database collation is not `_UTF8` (environment capability mismatch, Doctrine intent preserved).
- Platform parity: aligned `SQLitePlatform.getDateDiffExpression()` with Datazen/Doctrine (`JULIANDAY(..., 'start of day')` semantics) and corrected `PostgreSQLPlatform.getDateDiffExpression()` to `DATE(date1) - DATE(date2)` (fixes midnight-boundary functional date-diff parity).
- Functional/Types parity (module start): added Doctrine-based `Functional/Types/*` ports in `src/__tests__/functional/types/` for `AsciiStringTest`, `BigIntTypeTest`, `BinaryTest`, `DecimalTest`, `GuidTest`, `JsonTest`, `JsonObjectTest`, `JsonbTest`, `NumberTest` (Node-adapted), and `EnumTypeTest`. Validated the full new module against real `sqlite3`, `mysql`, `mariadb`, `postgresql`, and `sqlserver` targets.
- Platform/schema/runtime parity fixes surfaced by `Functional/Types`: aligned generic enum SQL with Datazen/Doctrine (`AbstractPlatform.getEnumDeclarationSQL()` now uses `VARCHAR` fallback + `ColumnValuesRequired` validation) and added the MySQL native `ENUM(...)` override in `AbstractMySQLPlatform`; added PostgreSQL `JSONB` declaration/type mapping (`Types.JSONB`) in `PostgreSQLPlatform`; extended metadata introspection parsing for MySQL/MariaDB enum values (`COLUMN_TYPE` -> `Column.values`) and ignored enum `length` noise during metadata mapping; enabled mysql2 functional connections with `supportBigNumbers` + `bigNumberStrings` to preserve bigint precision; and aligned comparator behavior with Doctrine by ignoring integer precision/scale metadata noise in `Comparator.compareColumns()`.
- Functional module parity (next batch): added Doctrine-based ports for `Functional/LockMode/NoneTest`, `Functional/ParameterTypes/AsciiTest`, and `Functional/SQL/ParserTest` in `src/__tests__/functional/lock-mode/none.test.ts`, `src/__tests__/functional/parameter-types/ascii.test.ts`, and `src/__tests__/functional/sql/parser.test.ts`. Added runtime-gated multi-platform behavior (SQLite in-memory skip for `LockMode`, SQL Server-only ASCII binding, MySQL/PostgreSQL parser cases).
- SQL parser parity: implemented Doctrine-style escaped question-mark token support across multi-pass SQL compilation by preserving `??` in `ExpandArrayParameters` and collapsing it at the parser/driver conversion boundary. This fixes PostgreSQL `Functional/SQL/ParserTest` (`jsonb ?? :key`) without weakening the functional test.
- MSSQL adapter parity: implemented `ParameterType.ASCII` typed binding as SQL Server `VARCHAR(n)` (not `NVARCHAR`) in the `mssql` adapter, which enables Doctrine-equivalent `Functional/ParameterTypes/AsciiTest` behavior (`sql_variant_property(..., 'BaseType') = varchar`).
- Test runner split + MariaDB default-value parity: introduced `vitest.unit.config.ts` and updated `package.json` so `bun run test` / `bun run test:coverage` run unit tests only (exclude `src/__tests__/functional/**`), while functional coverage remains in `bun run test:functional`. Also hardened MariaDB `COLUMN_DEFAULT` metadata normalization in `src/platforms/_internal/metadata-provider-utils.ts` to handle version-dependent unquoted MySQL-style escaped defaults (e.g. `\\n`, `\\r`, `\\\\`, `\\'`) and bare `NULL` values returned by MariaDB in CI, aligning introspection behavior with Datazen/Doctrine `Functional/Schema/DefaultValueTest` across the MariaDB matrix.
- CI workflow orchestration (Doctrine-inspired): added a top-level `.github/workflows/ci.yml` that orchestrates reusable `lint`, unit `test`, and per-platform functional workflows. `lint` and unit `test` now run in parallel, while all functional jobs (`sqlite3`, `mysql`, `mariadb`, `postgresql`, `sqlserver`) run only after the unit `test` job succeeds (`needs: test`). Converted child workflows to reusable/manual-only (`workflow_call` + `workflow_dispatch`) so they no longer trigger duplicate `push`/`pull_request` runs independently.
- Platform/driver parity: aligned PostgreSQL binary/varbinary declarations with Datazen/Doctrine (`BYTEA`), implemented PostgreSQL-specific date arithmetic interval SQL and `LOCATE(..., start)` `CASE` behavior, and aligned SQL Server BLOB declarations to `VARBINARY(MAX)`. In the MSSQL adapter, fixed metadata type-name detection (`column.type` is a function in `mssql`/tedious), normalized SQL Server temporal result values to preserve DB wall-clock semantics, and added typed `VARBINARY(MAX)` parameter binding (including `NULL`) for `ParameterType.BINARY`/`LARGE_OBJECT`. This removes the prior PostgreSQL (`BinaryDataAccessTest`, `DataAccessTest`) and SQL Server (`BlobTest`, `DataAccessTest`) functional failures without test-specific hacks. Validated with `bun run typecheck`, full functional suites on `postgresql` and `sqlserver`, `bun run format`, and `bun run test`.
- Functional/Schema default-value parity: aligned metadata default-value normalization in `src/platforms/_internal/metadata-provider-utils.ts` with Doctrine-intent cross-driver introspection behavior by handling PostgreSQL casted literals (`'...'::type`), SQL Server parenthesized/N-prefixed defaults (`(('...'))`, `N'...'`), and MariaDB quoted metadata literals with MySQL-style escape decoding. Preserved MySQL raw `COLUMN_DEFAULT` semantics (no over-decoding). Target validation across real `sqlite3`, `mysql`, `mariadb`, `postgresql`, and `sqlserver` now passes for `Functional/ConnectionTest`, `Functional/Schema/DefaultValueTest`, and `Functional/PrimaryReadReplicaConnectionTest`.
- Tests/Functional parity: replaced the root-level placeholder `src/__tests__/functional/primary-read-replica-connection.test.ts` with a Doctrine-based implementation of `Functional/PrimaryReadReplicaConnectionTest` (default replica reads, explicit primary switch, write-to-primary switching, `keepReplica` behavior, and close/reconnect state reset) using the multi-platform functional harness. Added a Node-adapter-specific runtime skip only for the charset inheritance case because the mysql2 adapter currently injects pre-created clients/pools, making connect-time charset inheritance unobservable functionally (the inheritance logic remains covered in unit tests).
- Functional test infrastructure: added `createFunctionalDriverManagerParams()` to `src/__tests__/functional/_helpers/functional-connection-factory.ts` so functional tests can construct real Datazen connections (including `PrimaryReadReplicaConnection`) from the same env/JSON configuration source without duplicating driver-specific client/pool setup.
- Tests/Functional parity: replaced the root-level placeholder `src/__tests__/functional/data-access.test.ts` with a Doctrine-based implementation of `Functional/DataAccessTest` (prepared fetch APIs, typed DateTime binds, array-parameter expansion, trim/locate/substring SQL expressions, date arithmetic expression matrix across bind/literal/expression modes, SQLite dynamic interval date math, and SQL injection-safe quoting). Added only small Node normalization helpers for row key casing and datetime value comparison.
- SQLite platform parity: aligned `src/platforms/sqlite-platform.ts` with Datazen/Doctrine for `getTrimExpression()` (`TRIM`/`LTRIM`/`RTRIM` semantics), `getLocateExpression()` (Doctrine-style `CASE ... ELSE 0` behavior for non-1 start offsets), and SQLite-specific `DATETIME(..., '+' || interval || ' UNIT')` date arithmetic expression generation.
- Platform parity: aligned `src/platforms/abstract-platform.ts` `multiplyInterval()` with Datazen/Doctrine by wrapping the expression in parentheses (`(<interval> * <multiplier>)`), which fixes SQLite week/quarter date arithmetic expressions built from interval concatenation.
- Tests/Functional parity: replaced the root-level placeholder `src/__tests__/functional/binary-data-access.test.ts` with a Doctrine-based implementation of `Functional/BinaryDataAccessTest`, covering binary parameter binding (`ParameterType::BINARY`), typed fetch variants (`Types::BINARY`), prepared statements, and native array-parameter expansion (`ArrayParameterType::BINARY`) with Node `Buffer`/`Uint8Array` normalization across drivers.
- Tests/Functional parity: replaced the root-level placeholder `src/__tests__/functional/blob.test.ts` with a Doctrine-based implementation of `Functional/BlobTest` for scalar BLOB/CLOB insert/select/update/null handling and "multiple LARGE_OBJECT bindings do not overwrite" behavior. Kept explicit `it.skip(...)` only for PHP stream-resource scenarios (`insert/update/bindValue` stream processing), which are PHP-specific and not part of Datazen's Node value model.
- Tests/Functional parity: replaced the root-level placeholder `src/__tests__/functional/foreign-key-constraint-violations.test.ts` with a Doctrine-based implementation of `Functional/ForeignKeyConstraintViolationsTest` (transactional, deferred PostgreSQL-only, savepoint-nesting, and commit/insert violation scenarios). The new file runs across the functional harness targets with runtime skips only where the Doctrine setup path is unsupported (SQLite FK `ALTER TABLE ... ADD CONSTRAINT`).
- Connection parity: aligned `Connection.transactional()` with Datazen/Doctrine by preserving the original callback/commit exception when rollback cleanup fails or the transaction is already inactive (prevents `NoActiveTransaction` from masking deferred constraint violations on PostgreSQL).
- SQLite parity: aligned `SQLitePlatform` with Datazen/Doctrine by making `getCreateForeignKeySQL()` / `getDropForeignKeySQL()` throw `NotSupported` instead of emitting unsupported generic `ALTER TABLE ... ADD CONSTRAINT` SQL.
- SQLite driver parity: changed `SQLite3Driver.connect()` to enable `PRAGMA foreign_keys=ON` via `query()` (non-DML path) so a fresh connection no longer pollutes `lastInsertId()` state, restoring Doctrine-style `NoIdentityValue` behavior in functional `WriteTest`. Updated `src/__tests__/driver/sqlite3-driver.test.ts` stubs for the connect-time PRAGMA callback flow.
- Tests/Functional parity: replaced the root-level placeholder skips in `src/__tests__/functional/{result-metadata,temporary-table}.test.ts` with Doctrine-based implementations (`ResultMetadataTest`, `TemporaryTableTest`). Added Node-adapted runtime skip only for the sqlite3 empty-result metadata case (driver metadata limitation on empty result sets). `TemporaryTableTest` now uses a direct functional connection (Doctrine single-session intent) to preserve session-scoped temp-table behavior and avoid pooled-session false failures.
- Result/Driver parity: aligned `Result.getColumnName()` with Doctrine by throwing `InvalidColumnIndex` (instead of leaking `RangeError`) for invalid metadata indexes, and updated coverage in `src/__tests__/result/result.test.ts`.
- Driver/Platform parity: preserved empty-result column metadata for mysql2 and mssql driver adapters (`src/driver/mysql2/connection.ts`, `src/driver/mssql/connection.ts`), added Doctrine-style MySQL temporary-table drop SQL (`DROP TEMPORARY TABLE`) in `src/platforms/abstract-mysql-platform.ts`, and added SQL Server temporary-table overrides (`CREATE TABLE`, `#temp` names) in `src/platforms/sql-server-platform.ts`.
- Tests/Functional parity: replaced the root-level `src/__tests__/functional/auto-increment-column.test.ts` placeholder skip with a Doctrine-based implementation (`AutoIncrementColumnTest`) using schema editors for setup and platform-specific assertions (`PostgreSQL`/`DB2` next-identity semantics). Added a SQL Server-specific batch adaptation for `IDENTITY_INSERT` (`ON; INSERT; OFF`) to preserve Doctrine behavior under the Node `mssql` pooled-session model. Validated on `sqlite3`, `mysql`, `postgresql`, and `sqlserver`.
- Connection async platform-resolution parity: made driver platform/version selection async-capable end-to-end (`Driver.getDatabasePlatform()` may return a promise; MySQL/PostgreSQL abstract drivers now await server-version providers) and added `Connection.resolveDatabasePlatform()` caching/guard integration across connect/execute paths. `Connection.createSchemaManager()` is now async and awaits platform resolution before schema-manager creation, removing the remaining sync/async mismatch for version-aware platforms in functional runs. Migrated functional schema-manager setup helpers/tests (`FunctionalTestCase`, `PortabilityTest`, `ExceptionTest`) to the awaited API.
- Connection/Functional parity (broader multi-driver cleanup): aligned nested transaction/savepoint behavior in `src/connection.ts` with Datazen/Doctrine (`create/release/rollbackSavepoint()` capability checks and no-op nested `commit()` when `supportsReleaseSavepoints() === false`, which fixes SQL Server nested savepoint functional cases). Extended mysql2 lost-connection classification for Node socket-level disconnect errors (`EPIPE`, `ECONNRESET`, "socket has been ended by the other party") so session-kill transaction tests map to `ConnectionLost`. Updated `src/__tests__/connection/connection-transaction.test.ts` to assert the Doctrine-correct no-release-savepoint semantics. Validated the currently implemented root-level functional suite against real `mysql`, `mariadb`, `postgresql`, and `sqlserver` targets (all green with only intentional placeholder skips).
- Driver/Functional parity (connection-loss + pg direct client): fixed Node-driver connection-loss behavior to match Datazen/Doctrine intent across functional tests. MySQL/mysql2 exception conversion now maps additional lost-connection cases (`2013`, protocol closed-state strings/messages) to `ConnectionLost`, `src/__tests__/functional/connection/connection-lost.test.ts` now pins a pooled MySQL session via an open transaction to reproduce `wait_timeout` loss while preserving reconnect semantics, and `src/__tests__/functional/transaction.test.ts` uses dedicated functional connections for session-kill scenarios (plus a longer timeout for the nested savepoint walkthrough on slower MySQL/MariaDB containers). Also fixed mysql2 direct-connection transaction cleanup (`release()` only on pooled borrowed connections) and made `PgDriver`/`PgConnection` distinguish pooled vs direct clients explicitly (fixing `pg.Client` misclassification, async fatal-event handling during kill-session tests, and direct-client transaction behavior). Added Doctrine-parity SQL Server runtime skip for `FetchTest::testFetchAllKeyValueWithLimit()` and removed a non-Doctrine extra `COUNT(*)` type assertion from `WriteTest`.
- Tests/Functional naming parity: updated functional tests to use the port-native `Connection.convertToNodeValue(...)` API (instead of the legacy `convertToPHPValue(...)` alias) in `src/__tests__/functional/{boolean-binding,type-conversion}.test.ts`.
- Tests/Functional parity (root-level Doctrine files): added exact-match `src/__tests__/functional/*.test.ts` coverage for the remaining root-level Doctrine functional files, including real multi-platform-capable ports for `NamedParametersTest`, `BooleanBindingTest`, `FetchBooleanTest`, `LikeWildcardsEscapingTest`, `ModifyLimitQueryTest`, `ExceptionTest`, `PortabilityTest`, and `TypeConversionTest`, plus exact-match placeholder files (explicit skips with Doctrine-scoped reasons) for root-level scenarios that still need driver/topology-specific implementation (`AutoIncrementColumnTest`, `BinaryDataAccessTest`, `BlobTest`, `DataAccessTest`, FK/unique violation and exception tests, `PrimaryReadReplicaConnectionTest`, `ResultMetadataTest`, `TemporaryTableTest`).
- Tests/Functional multi-platform: implemented Doctrine-style `TransactionTest` connection-loss scenarios in `src/__tests__/functional/transaction.test.ts` using runtime platform gating (`AbstractMySQLPlatform` / `PostgreSQLPlatform`) plus a privileged side connection to kill the current session, and kept SQLite/other platforms as runtime skips. Added privileged functional connection support in `src/__tests__/functional/_helpers/functional-connection-factory.ts` (JSON/env-driven `privilegedConnection` overrides), exposed `TestUtil.getPrivilegedConnection()` in `src/__tests__/test-util.ts`, and added config coverage in `src/__tests__/functional/_helpers/functional-connection-factory-config.test.ts`.
- Tests/Functional review cleanup: updated currently ported functional suites to remove stale SQLite-only labels and switched savepoint-dependent tests in `src/__tests__/functional/{connection,transaction}.test.ts` to runtime skip gating so the same files run cleanly under the new multi-platform harness.
- Tests/Functional parity: replaced the placeholder `src/__tests__/functional/connection/connection-lost.test.ts` skip with a Doctrine-style implementation gated to MySQL-family targets (`mysql`/`mariadb`). The test now sets `SESSION wait_timeout`, waits for expiry, asserts `ConnectionLost`, and verifies a subsequent query succeeds (reconnect path), while remaining skipped on non-MySQL platforms.
- Tests/TestUtil parity: added a shared Doctrine-style `src/__tests__/test-util.ts` with `TestUtil.generateResultSetQuery(...)` and migrated `src/__tests__/functional/connection/fetch.test.ts` to use it (matching Doctrine’s `FetchTest` -> `TestUtil` mental model). Added coverage in `src/__tests__/test-util.test.ts` and updated `src/__tests__/tools/test-suite-structure.test.ts` to track `TestUtil.php` as a shared test utility.
- Tests/Functional refactor: migrated the currently ported functional tests to `src/__tests__/functional/_helpers/functional-test-case.ts` (`useFunctionalTestCase()`) and deleted the legacy `src/__tests__/functional/_helpers/sqlite-functional-connection.ts` helper. Kept `generateResultSetQuery()` out of `FunctionalTestCase` (Doctrine parity); it now lives in shared `src/__tests__/test-util.ts` (`TestUtil`) rather than on the functional base helper.
- CI/Functional matrix coverage: expanded functional CI workflows to run database-version matrices and feed `DATAZEN_FUNCTIONAL_SERVER_VERSION` from the matrix in `test-{mariadb,mysql,postgresql,sqlserver}.yaml`. Added MariaDB `10.4/10.5/10.6/10.11/11.4/11.8/12.1`, PostgreSQL `10/16/17`, MySQL `5.7/8.0/8.4/9.6`, and SQL Server `2022/2025` (via image-tag mapping).
- Tests/Functional parity helpers: expanded `src/__tests__/functional/_helpers/functional-test-case.ts` with Doctrine-style schema/name/index/constraint utility methods (`dropSchemaIfExists`, quoted-name conversion helpers, list-contains assertions, and index/PK/FK normalization assertions) and added coverage in `src/__tests__/functional/_helpers/functional-test-case.test.ts`. Restored `src/__tests__/functional/_helpers/sqlite-functional-connection.ts` compatibility helpers used by the existing functional test ports.
- CI/Functional parity workflow: replaced the single `.github/workflows/functional.yml` job with Doctrine-style per-platform workflows (`test-mysql.yaml`, `test-mariadb.yaml`, `test-postgresql.yaml`, `test-sqlserver.yaml`, `test-sqlite3.yaml`). CI now uses GitHub Actions `services` instead of `docker compose`, keeping `compose.yaml` for local development only.
- Tests/Functional infrastructure: added JSON-based functional connection profiles in `ci/github/vitest/*.json` and taught `src/__tests__/functional/_helpers/functional-connection-factory.ts` to load `DATAZEN_FUNCTIONAL_CONFIG_FILE` (with env vars still overriding JSON values). Added unit coverage in `src/__tests__/functional/_helpers/functional-connection-factory-config.test.ts`.
- Tests/Functional infrastructure: added a reusable `FunctionalTestCase`-style helper (`src/__tests__/functional/_helpers/functional-test-case.ts`) and an env-driven real-connection factory (`src/__tests__/functional/_helpers/functional-connection-factory.ts`) for `sqlite3`, `mysql2`, `pg`, and `mssql`, plus `compose.yaml`, `scripts/run-functional-tests.mjs`, functional CI workflows, and the `bun run test:functional` script entry for platform-selected functional runs.
- SQLite platform parity: implemented Doctrine-style SQLite autoincrement DDL generation in `src/platforms/sqlite-platform.ts` (`INTEGER PRIMARY KEY AUTOINCREMENT` inline on auto-increment integer columns, with table-level PK suppression and SQLite-safe index emission during `CREATE TABLE`), fixing functional `WriteTest` failures when tables are created via schema editors instead of raw SQL.
- Tests/Platforms parity: added a SQLite regression test in `src/__tests__/platforms/sqlite-platform.test.ts` to lock the autoincrement create-table behavior and prevent duplicate primary key clauses.
- Tests/Functional parity: added the exact-match scaffold `src/__tests__/functional/connection/connection-lost.test.ts` for Doctrine `tests/Functional/Connection/ConnectionLostTest.php`, with an explicit MySQL-only skip because the current functional harness is SQLite in-memory and cannot reproduce `wait_timeout`-based connection loss.
- Tooling/Audit: whitelisted `Functional/Connection/ConnectionLostTest.php` in `scripts/doctrine-test-parity-report.mjs` so it is tracked as matched (file parity) rather than hidden by the blanket functional exclusion.
- Tests/Functional parity: ported additional Doctrine functional files into SQLite-backed Vitest equivalents:
  - `tests/Functional/WriteTest.php` -> `src/__tests__/functional/write.test.ts`
  - `tests/Functional/TransactionTest.php` -> `src/__tests__/functional/transaction.test.ts`
  - `tests/Functional/ConnectionTest.php` -> `src/__tests__/functional/connection.test.ts`
  The new files preserve Doctrine intent for portable transaction/DML/error paths and use explicit `it.skip(...)` for MySQL/PostgreSQL session-kill tests, params-based reconnect/bootstrap scenarios, persistent connections, and other non-SQLite/PHP-native branches.
- Tests/Functional parity: added SQLite-adapted coverage for Doctrine `WriteTest` key/value type maps (`datetime`, `integer`) using built-in type registration, `getEmptyIdentityInsertSQL()` identity inserts, and `IS NULL` criteria update/delete behavior.
- Tooling/Audit: expanded the partial functional whitelist in `scripts/doctrine-test-parity-report.mjs` to count `Functional/{ConnectionTest,WriteTest,TransactionTest}.php` as matched.
- Tests/Functional parity: ported Doctrine `tests/Functional/{ResultTest,StatementTest}.php` into unified Vitest files `src/__tests__/functional/{result,statement}.test.ts` using the SQLite functional harness. Preserved the Doctrine statement-reuse / incomplete-fetch / parameter-order / prepared execute scope, with explicit skips for PHP/driver-specific branches (native sqlite3 invalid named parameter reporting, PHP BLOB stream conversion, redundant-parameter error variability).
- Tests/Functional parity: adapted Doctrine `StatementTest::testParameterBindingOrder()` for Node `sqlite3` row-object semantics by aliasing duplicate `LENGTH(?)` expressions (`len1`/`len2`) while preserving the same binding-order contract.
- Tooling/Audit: expanded the partial functional whitelist in `scripts/doctrine-test-parity-report.mjs` to count `Functional/{ResultTest,StatementTest}.php` as matched (`Functional: 4 matched / 113 excluded`).
- Tests/Functional parity: started the functional test port with a real SQLite-backed Vitest harness (`src/__tests__/functional/_helpers/sqlite-functional-connection.ts`) using the existing `sqlite3` driver adapter and in-memory databases, then ported Doctrine `tests/Functional/Connection/{FetchTest,FetchEmptyTest}.php` into `src/__tests__/functional/connection/{fetch,fetch-empty}.test.ts` with Node-adapted async iterator assertions (`Object.fromEntries()` / async collection) while preserving the same fetch/empty-result/NoKeyValue coverage intent.
- Tooling/Audit: updated `scripts/doctrine-test-parity-report.mjs` to treat the functional suite as partially ported (explicitly counting matched files before the blanket functional exclusion), bringing the current snapshot to `Matched: 114`, `Fuzzy: 0`, `Missing: 0`, `Excluded: 131` with `Functional: 2 matched / 115 excluded`.
- Tests/Parity structure: updated `src/__tests__/tools/test-suite-structure.test.ts` functional-directory exclusion note to reflect the new partial functional port bootstrap instead of a fully unported status.
- Tests/Skip review (Doctrine-reference pass): ported additional non-PHP-specific skipped cases using `references/dbal-full` as the source of truth and removed placeholder skips in `src/__tests__/schema/{index,unique-constraint,foreign-key-constraint}.test.ts`. Added Doctrine-derived coverage for index partial/length-aware fulfillment, invalid indexed-column/type/predicate states, invalid unique-constraint column-name parsing, and invalid foreign-key column/table/match/action/deferrability states.
- Schema parity: aligned `src/schema/index.ts`, `src/schema/unique-constraint.ts`, and `src/schema/foreign-key-constraint.ts` with Datazen/Doctrine invalid-state behavior for the above validation paths (while still leaving Doctrine deprecation-only signaling out of scope in Node). `Index.getIndexedColumns()` now validates parseability/length rules, preserves quoted column metadata, and `Index.isFulfilledBy()` now compares partial predicates and non-null indexed lengths.
- Driver/IBMDB2 parity: implemented `src/driver/ibm-db2/data-source-name.ts` and replaced the scaffold in `src/__tests__/driver/ibm-db2/data-source-name.test.ts` with Doctrine `DataSourceNameTest` formatting coverage (`fromConnectionParameters()` + pass-through uncataloged DSN strings).
- Tests/Comparator parity: expanded the unified `src/__tests__/schema/abstract-comparator-test-case.test.ts` using `references/dbal-full/tests/Schema/AbstractComparatorTestCase.php` as the implementation/test reference, adding non-Node-specific Doctrine matrix coverage for FQN/default-schema table matching, namespace diffs, auto-increment helper sequence filtering, multi-column rename heuristics, moved foreign-key target diffs, FK + renamed-local-column schema diffs, column-name case-insensitive comparison, and the `columnDefinition` no-op schema-diff case. Removed the remaining placeholder skip in this file (`29/29` passing).
- Comparator/Schema parity: fixed `src/schema/comparator.ts#compareSchemas()` to match tables/sequences via normalized shortest-name lookup maps (instead of `Schema.getTable()/hasTable()` on shortened names), restoring Doctrine-style FQN/default-schema equivalence. Also ignored namespace diff entries that are aliases of either schema's configured default namespace (for example `foo` vs implicit default `foo`) to align with Doctrine `AbstractComparatorTestCase` expectations.
- Comparator/Schema parity: started core `AbstractComparatorTestCase` parity work by implementing Doctrine-style case-insensitive schema asset matching in `src/schema/comparator.ts#compareSchemas()` (tables/sequences via schema lookups and shortest names), plus index rename detection and configurable modified-index reporting in `compareTables()` / `detectRenamedIndexes()`.
- Tests/Comparator parity: expanded `src/__tests__/schema/abstract-comparator-test-case.test.ts` with Doctrine-derived coverage for table/sequence case-insensitive schema comparison, index rename detection (enabled/disabled/ambiguous), and modified-index reporting (enabled/disabled). Full comparator matrix remains partially skipped for column rename/FK edge cases.
- Tests/Skip review: audited remaining `it.skip(...)` cases before functional test work and ported non-PHP Doctrine gaps in `TableTest` / `MySQLSchemaTest`, reducing skipped tests from `31` to `28` (`1248` passed). Added unified Doctrine `TableTest` asset-name normalization matrix coverage and exact auto-generated rename semantics (`primary`, CRC32-based identifiers) in `src/__tests__/schema/table.test.ts`.
- Schema/Table parity: aligned `src/schema/abstract-asset.ts#_generateIdentifierName()` with Datazen/Doctrine (`crc32`-based identifier hash), and updated `src/schema/table.ts` to use Doctrine-compatible auto-generated names for indexes/unique/FK constraints (`[$tableName, ...columns]` hashing), default unnamed primary key name `primary`, and Doctrine-style `renameIndex()` re-add semantics.
- Platforms/Schema SQL parity: implemented baseline `src/platforms/abstract-platform.ts#getAlterTableSQL()` support for index/foreign-key diffs (with explicit `NotSupported` for column diffs still requiring platform-specific syntax), enabling port of Doctrine `MySQLSchemaTest::testClobNoAlterTable` in `src/__tests__/schema/platforms/mysql-schema.test.ts`.
- Tests/Schema manager: updated `src/__tests__/schema/schema-manager.test.ts` shim expectations to reflect newly supported `alterTable()` index/FK diff SQL generation while still asserting column-diff paths reject as unsupported.
- Platforms/Metadata: converted `schema/metadata/MetadataProvider` to an async contract (`Promise<...[]>`) and added async query adapter contract `src/platforms/_internal/async-query-connection.ts`, so metadata-provider methods are awaited end-to-end instead of exposing synchronous iterables over DB-backed operations.
- Platforms/Metadata: wired `createMetadataProvider()` in `MySQL`, `MariaDB`, `PostgreSQL`, `SQLite`, `SQLServer`, and `DB2` platform classes to their concrete provider implementations.
- Platforms/Metadata: expanded metadata-provider implementations for `MySQL`, `PostgreSQL`, `SQLite`, `SQLServer`, and `DB2` beyond top-level names/views to include column metadata, index-column metadata, primary-key columns, foreign-key columns, and table options (where applicable / supported), with explicit `NotSupported` retained only for truly unsupported vendor features (for example MySQL/SQLite database schema/sequences).
- Tests/Platforms: upgraded `src/__tests__/platforms/metadata-providers.test.ts` to async execution and added unified cross-platform coverage for `createMetadataProvider()` wiring plus expanded metadata row mapping scenarios (columns/indexes/PK/FK/options).
- Platforms/MySQL: implemented missing Doctrine-like MySQL platform subfolder components in `src/platforms/mysql/*`, including `Comparator`, `DefaultTableOptions`, `CharsetMetadataProvider` / `CollationMetadataProvider` interfaces, and caching/connection provider implementations (`charset-metadata-provider/*`, `collation-metadata-provider/*`). Added adapted Doctrine-based tests for MySQL comparator behavior, MariaDB JSON comparison semantics, and caching/provider query behavior in `src/__tests__/platforms/mysql/**/*.test.ts`.
- Platforms/Comparators: implemented platform comparator classes for `SQLite` and `SQLServer` (`src/platforms/{sqlite,sqlserver}/comparator.ts`) plus placeholder parity comparators for `DB2` and `PostgreSQL` (`src/platforms/{db2,postgresql}/comparator.ts`), and replaced comparator parity scaffolds with real tests in `src/__tests__/platforms/{mysql,sqlite,sql-server}/*.test.ts`.
- Platforms/Metadata: added Doctrine-like metadata-provider class surfaces for MySQL/PostgreSQL/DB2/SQLite/SQL Server (`src/platforms/{mysql,postgresql,db2,sqlite,sqlserver}/*metadata-provider*.ts`) and `SQLite` foreign-key details value object (`src/platforms/sqlite/sqlite-metadata-provider/foreign-key-constraint-details.ts`). Current implementation uses a synchronous query-executor contract (`src/platforms/_internal/sync-query-connection.ts`) for safe/testable usage without changing the project’s async `Connection` metadata interfaces yet; unported metadata-provider methods throw explicit `NotSupported`.
- Tests/Platforms: added `src/__tests__/platforms/metadata-providers.test.ts` to exercise cross-platform metadata-provider row mapping for supported methods and explicit `NotSupported` behavior for unported methods.
- Tests/Parity: replaced the remaining fuzzy `Schema/*` mappings with unified, exact-match Vitest files `src/__tests__/schema/schema.test.ts` and `src/__tests__/schema/table.test.ts`, covering Doctrine `SchemaTest` / `TableTest` scope in one file each (with explicit `it.skip(...)` markers for currently unsupported Node-port gaps such as quoted identifier normalization loops, PHP deprecation harness assertions, and PHP clone semantics).
- Tooling/Audit: Doctrine parity report is now exact at the file level for in-scope modules: `Matched: 112`, `Fuzzy: 0`, `Missing: 0`, `Excluded: 133` (`161` Datazen test files); `Schema` is now `32/32 matched`.
- Tests/Parity: completed the remaining Doctrine/Datazen file-level parity batches for `Schema/*`, `Driver/*`, and `Platforms/*` by porting/adapting the last missing test files into Vitest, including `Schema/{AbstractComparatorTestCase,Platforms/MySQLSchemaTest}.php`, `Driver/{AbstractDriverTestCase,AbstractOracleDriverTestCase,AbstractSQLServerDriverTestCase,AbstractSQLiteDriverTestCase,IBMDB2/DataSourceNameTest}.php`, and the full remaining `Platforms/*` unit-test block (17 files) with explicit parity scaffolds/skips where Doctrine-only comparator/collation internals are not implemented in the Node port.
- Tests/Parity: added shared platform parity scaffolding (`src/__tests__/platforms/_helpers/platform-parity-scaffold.ts`) and Node-adapted assertions for MySQL schema SQL/comparator behavior where local platform output differs in SQL shape or no-diff return style (`null` vs empty diff object).
- Tooling/Audit: current Doctrine parity snapshot is `Matched: 110`, `Fuzzy: 2`, `Missing: 0`, `Excluded: 133` (`159` Datazen test files). File-level parity is complete for implemented/in-scope modules; remaining fuzzy matches are alias mappings around `Schema/SchemaTest.php` and `Schema/TableTest.php`.
- Tests/Parity: completed the remaining `Types/*` temporal/date batch by porting Doctrine `tests/Types/{BaseDateTypeTestCase,DateTest,DateImmutableTypeTest,DateIntervalTest,DateTimeTest,DateTimeImmutableTypeTest,DateTimeTzTest,DateTimeTzImmutableTypeTest,TimeTest,TimeImmutableTypeTest,VarDateTimeTest,VarDateTimeImmutableTypeTest}.php` into Vitest (`src/__tests__/types/*.test.ts`) with a shared `BaseDateTypeTestCase` parity scaffold helper, Node-adapted timezone token assertions (`P` offset format), JS-millisecond precision assertions for microseconds, and explicit skips for PHP-only immutability / `DateInterval` object semantics.
- Tooling/Audit: current Doctrine parity snapshot after completing `Types/*` is `Matched: 86`, `Fuzzy: 2`, `Missing: 24`, `Excluded: 133` (`135` Datazen test files); `Types` is now `33/33 matched`.
- Tests/Parity: ported additional low-risk `Types/*` files from Doctrine into Vitest: `tests/Types/{BinaryTest,BlobTest,TypeRegistryTest,TypeTest,TypeWithConstructor}.php` (`src/__tests__/types/{binary,blob,type-registry,type,type-with-constructor}.test.ts`), including a Node-adapted constructor-argument guard helper to preserve the `Type::addType()` argument-count failure contract under JavaScript runtime semantics.
- Tooling/Audit: current Doctrine parity snapshot after this follow-up Types batch is `Matched: 74`, `Fuzzy: 2`, `Missing: 36`, `Excluded: 133` (`123` Datazen test files); `Types` is now `21/33 matched` with `12` remaining (mostly `BaseDateTypeTestCase` + date/time family).
- Tests/Parity: started the `Types/*` parity rollout by porting Doctrine `tests/Types/{AsciiStringTest,BooleanTest,ConversionExceptionTest,DecimalTest,FloatTest,GuidTypeTest,IntegerTest,JsonObjectTest,JsonTest,NumberTest,SmallFloatTest,SmallIntTest,StringTest}.php` and `tests/Types/Exception/{SerializationFailedTest,TypeAlreadyRegisteredTest,TypeNotRegisteredTest}.php` into Vitest (`src/__tests__/types/**/*.test.ts`), with explicit Node-adapted assertions for JSON float formatting, Buffer-backed JSON input (resource-like parity), and a skipped `NumberTest` case for Doctrine's PHP `BcMath\\Number`-specific invalid DB-value contract.
- Tooling/Audit: current Doctrine parity snapshot after this Types batch is `Matched: 69`, `Fuzzy: 2`, `Missing: 41`, `Excluded: 133` (`118` Datazen test files); `Types` is now `16/33 matched` with `17` remaining. Also tightened `scripts/doctrine-test-parity-report.mjs` fuzzy matching to avoid `Types/TypeTest.php` false positives against files like `guid-type.test.ts`.
- Tests/Parity: ported Doctrine `tests/Schema/{MySQLInheritCharsetTest,SQLiteSchemaManagerTest}.php` into Vitest (`src/__tests__/schema/{mysql-inherit-charset,sqlite-schema-manager}.test.ts`), preserving charset-inheritance and default-database FK introspection intent with local SQL-shape adaptation where the current platform output differs.
- Schema/MySQL: `src/schema/mysql-schema-manager.ts` now inherits `charset` from connection params into `SchemaConfig` default table options; `src/platforms/abstract-mysql-platform.ts` now appends `DEFAULT CHARACTER SET ...` for table-level charset options in `CREATE TABLE` SQL generation.
- Schema: `src/schema/abstract-schema-manager.ts#listTableForeignKeys()` now routes through `fetchForeignKeyColumns()` / `_getPortableTableForeignKeysList()` using the current database name (with a fallback to prior introspection behavior if a driver-specific implementation throws), aligning better with Datazen/Doctrine schema-manager flow.
- Tooling/Audit: current Doctrine parity snapshot after this batch is `Matched: 53`, `Fuzzy: 2`, `Missing: 57`, `Excluded: 133` (`102` Datazen test files); `Schema` is now down to `2` missing files (`AbstractComparatorTestCase`, `Platforms/MySQLSchemaTest`).
- Tests/Parity: ported Doctrine `tests/Schema/{AbstractAssetTest,AbstractNamedObjectTest,AbstractOptionallyNamedObjectTest}` and `tests/Schema/Collections/{UnqualifiedNamedObjectSetTest,OptionallyUnqualifiedNamedObjectSetTest}` into Vitest (`src/__tests__/schema/{abstract-asset,abstract-named-object,abstract-optionally-named-object}.test.ts`, `src/__tests__/schema/collections/*.test.ts`) with explicit skips for Doctrine deprecation-only / PHP-semantics-only scenarios.
- Schema/Collections: aligned object-set behavior with Doctrine-style contracts:
  - `src/schema/collections/unqualified-named-object-set.ts` now accepts objects exposing either `getObjectName()` or `getName()` and supports variadic constructor initialization
  - `src/schema/collections/optionally-unqualified-named-object-set.ts` now preserves insertion order across named/unnamed entries (including rename-to-null flows) and supports variadic constructor initialization
- Schema: `src/schema/abstract-named-object.ts` now throws `InvalidState.objectNameNotInitialized()` on empty names, and `src/schema/abstract-optionally-named-object.ts` now treats empty-string names as unnamed (`null` semantics), improving parity with Datazen/Doctrine optional-name behavior.
- Tooling/Audit: current Doctrine parity snapshot after this batch is `Matched: 51`, `Fuzzy: 2`, `Missing: 59`, `Excluded: 133` (`100` Datazen test files).
- Tests/Parity: ported Doctrine `tests/Schema/{ForeignKeyConstraintEditorTest,PrimaryKeyConstraintEditorTest,UniqueConstraintEditorTest,ForeignKeyConstraintTest,PrimaryKeyConstraintTest,UniqueConstraintTest}.php` into Vitest (`src/__tests__/schema/*constraint*.test.ts`), preserving supported behavioral contracts and marking deprecation/invalid-state-only Node gaps with explicit skips.
- Schema: improved constraint parity surfaced by the new tests:
  - `src/schema/foreign-key-constraint.ts` now exposes parsed `getObjectName()`, preserves quoted referenced-table names via parser-based `getReferencedTableName()`, and normalizes `RESTRICT` string actions to `NO ACTION` for `onDelete()`/`onUpdate()`
  - `src/schema/primary-key-constraint-editor.ts` now defaults new primary-key constraints to clustered (`true`), aligning with Datazen/Doctrine editor semantics
  - `src/schema/unique-constraint.ts` now returns `null` for unnamed constraints (instead of `""`) from `getObjectName()`, with corresponding null-safe updates in `src/schema/{table,table-editor}.ts`
- Tests/Parity: updated `src/__tests__/schema/table-editor.test.ts` and `src/__tests__/schema/schema-name-introspection-parity.test.ts` to reflect the aligned primary-key clustered default and normalized foreign-key `RESTRICT`/`NO ACTION` behavior.
- Tooling/Audit: current Doctrine parity snapshot after this batch is `Matched: 46`, `Fuzzy: 2`, `Missing: 64`, `Excluded: 133` (`95` Datazen test files).
- Tests/Parity: ported Doctrine `tests/Schema/{ColumnEditorTest,IndexEditorTest,SequenceEditorTest,TableEditorTest}.php` into Vitest (`src/__tests__/schema/{column-editor,index-editor,sequence-editor,table-editor}.test.ts`) with TypeScript/Node-adapted assertions that preserve Doctrine behavioral intent.
- Schema: improved editor/runtime parity surfaced by the new tests:
  - `src/schema/column-editor.ts` now auto-registers built-in Datazen types before `setTypeName()`
  - `src/schema/{column,index,table}.ts` parsed `getObjectName()` now preserves quoted-name intent for editor-created quoted identifiers
  - `src/schema/index.ts#getIndexedColumns()` now preserves quoted column-name metadata via `UnqualifiedName`
  - `src/schema/sequence-editor.ts#setCacheSize()` now rejects negative values eagerly (Doctrine-style timing)
  - `src/schema/table-editor.ts` now rejects duplicate column/index renames immediately, preserves primary-key replacement semantics during `edit()`/`create()` flows, and keeps nonclustered PK intent when materializing tables
  - `src/schema/table.ts` now exposes parsed `getObjectName()` and preserves primary-key constraint state in `edit()`
- Tooling/Audit: current Doctrine parity snapshot after this batch is `Matched: 40`, `Fuzzy: 2`, `Missing: 70`, `Excluded: 133` (`89` Datazen test files).
- Tests/Parity: ported Doctrine `tests/Schema/{ColumnTest,IndexTest}.php` into Vitest (`src/__tests__/schema/{column,index}.test.ts`), including supported behavioral contracts plus explicit `it.skip(...)` markers for deprecation-only/not-yet-aligned scenarios.
- Schema: added parsed `getObjectName()` to `src/schema/column.ts` and `src/schema/index.ts`, made `Index` option access/predicate reads case-insensitive (`where`/`WHERE`), and aligned `Index.isFulfilledBy()` so a matching primary key can fulfill regular/unique index requirements (Doctrine-style directionality).
- Tooling: added explicit Doctrine parity aliases for `Schema/ColumnTest.php` and `Schema/IndexTest.php` in `scripts/doctrine-test-parity-report.mjs`; current audit snapshot is `Matched: 36`, `Fuzzy: 2`, `Missing: 74`, `Excluded: 133`.
- Tests/Parity: ported additional low-risk schema unit tests (`Schema/IdentifierTest`, `Schema/SequenceTest`, `Schema/ViewTest`, and `Schema/TableDiffTest`) into Vitest (`src/__tests__/schema/{identifier,sequence,view,table-diff}.test.ts`), restoring coverage for parsed object-name helpers and unnamed dropped-FK validation intent.
- Schema: added Doctrine-style parsed `getObjectName()` helpers to `src/schema/{identifier,sequence,view}.ts` and aligned `src/schema/table-diff.ts#getDroppedForeignKeyConstraintNames()` to throw `InvalidState` for unnamed dropped foreign keys.
- Tests/Parity: ported Doctrine schema unit tests for `Schema/Name/*` and `Schema/Index/IndexedColumnTest` into Vitest (`src/__tests__/schema/name/**/*.test.ts`, `src/__tests__/schema/index/indexed-column.test.ts`), and marked `Connection/CachedQueryTest` as an explicit cache-layer exclusion in `scripts/doctrine-test-parity-report.mjs`.
- Tooling: tightened `scripts/doctrine-test-parity-report.mjs` fuzzy matching to compare only within the expected target directory, reducing false positives like `Schema/ColumnTest` incorrectly matching `schema/index/indexed-column.test.ts`.
- Tests/Parity: ported Doctrine `tests/Portability/{ConnectionTest,OptimizeFlagsTest,ResultTest,StatementTest}.php` into Vitest (`src/__tests__/portability/*.test.ts`), and aligned `src/portability/result.ts` wrapper behavior with Datazen/Doctrine delegation semantics for `fetchAllNumeric()`, `fetchAllAssociative()`, and `fetchFirstColumn()`.
- Tests/Parity: marked `tests/Tools/Console/*` as an explicit out-of-scope exclusion in `scripts/doctrine-test-parity-report.mjs` (Doctrine console integration is not part of the Node port) and brought `Portability` parity to a full file-level match in the current audit.
- Tests/Parity: ported Doctrine `tests/Exception/DriverRequiredTest.php` and `tests/Query/Expression/{CompositeExpressionTest,ExpressionBuilderTest}.php` into Vitest (`src/__tests__/exception/driver-required.test.ts`, `src/__tests__/query/expression/*.test.ts`) and tightened `scripts/doctrine-test-parity-report.mjs` aliasing/fuzzy matching (notably `PgSQL`/`SQLSrv` driver test mappings).
- Tests/Parity: added a best-effort Doctrine-vs-Datazen test inventory audit script (`scripts/doctrine-test-parity-report.mjs`) and expanded `src/__tests__/tools/test-suite-structure.test.ts` to enforce top-level/root-level Doctrine test structure parity with explicit PHP-specific exclusions.
- Tests/Root parity: added Doctrine root-level parity test files `src/__tests__/connection.test.ts`, `src/__tests__/driver-manager.test.ts`, and `src/__tests__/exception.test.ts` to mirror core `ConnectionTest`, `DriverManagerTest`, and `ExceptionTest` intent in the TypeScript/Vitest suite.
- CI/Tests: added Vitest coverage support (`@vitest/coverage-v8`, `bun run test:coverage`, `vitest.config.ts` coverage reporters/output) and updated `.github/workflows/test.yml` to publish the generated `coverage/` report as a downloadable GitHub Actions artifact (`vitest-coverage-report`).
- Tests/PgSQL: added dedicated `src/__tests__/driver/pgsql-exception-converter.test.ts` coverage for PostgreSQL exception-converter query metadata extraction, `0A000` truncate vs non-truncate behavior, `ConnectionLost` message precedence, and string-code connection heuristics.
- Driver/SQLite: aligned `src/driver/api/sqlite/exception-converter.ts` closer to Datazen/Doctrine message-based conversion rules (lock wait timeout, unique/not-null/table/column ambiguity and syntax cases, readonly DB, connection-open failure, and FK violations) while retaining code-based fallbacks; added SQLite converter coverage in `src/__tests__/driver/driver-exception-converter.test.ts`.
- Driver/SQLServer: aligned `src/driver/api/sql-server/exception-converter.ts` closer to Datazen/Doctrine numeric-code parity by splitting `207/208/209` into invalid-field/table-not-found/non-unique-field mappings and adding `2714` table-exists plus `3701`/`15151` database-object-not-found mappings; expanded SQL Server converter coverage in `src/__tests__/driver/driver-exception-converter.test.ts`.
- Driver/PgSQL: expanded `src/driver/api/pgsql/exception-converter.ts` toward Datazen/Doctrine SQLSTATE parity (deadlock `40001`, truncate-specific `0A000` FK mapping, database/schema/table existence states, field-name ambiguity/invalid states, explicit `08006`, and `ConnectionLost` message heuristic) with pg coverage added to `src/__tests__/driver/driver-exception-converter.test.ts`.
- Driver/OCI: aligned `src/driver/api/oci/exception-converter.ts` closer to Datazen/Doctrine by adding `ORA-00960` non-unique-field mapping and wrapping `ORA-02091` as `TransactionRolledBack` with converted nested cause; expanded OCI converter tests to cover the full current Oracle code mapping set.
- Tests/Db2: expanded `src/__tests__/driver/ibmdb2-exception-converter.test.ts` to assert every Doctrine/Datazen IBM Db2 SQLCODE mapping branch currently handled by `src/driver/api/ibmdb2/exception-converter.ts` (including FK, connection, and field/table cases).
- Driver/MySQL: expanded `src/driver/api/mysql/exception-converter.ts` to cover additional Datazen/Doctrine MySQL error-code mappings (database/table exists-not-found, lock wait timeout, invalid/non-unique field names, and `ConnectionLost` for `2006`/`4031`) with added converter tests in `src/__tests__/driver/driver-exception-converter.test.ts`.
- Documentation: synced core docs with the current Node async API boundaries (async `Connection`/`Statement`/`QueryBuilder` execution, sync `Result` fetch/iterate usage), corrected stale transaction isolation notes, refreshed driver contract examples, and added `docs/parity-matrix.md` with Doctrine-to-Datazen async translation guidance.
- Cleanup: removed all currently unused private class members flagged by Biome (`lint/correctness/noUnusedPrivateClassMembers`) across platform/schema/connection parity placeholder code, and cleaned follow-up unused imports so `bun run format` runs with zero warnings.
- Exception: added `MalformedDsnException::new()` parity factory in `src/exception/malformed-dsn-exception.ts` with mirrored coverage in `src/__tests__/exception/malformed-dsn-exception.test.ts`, clearing the remaining non-excluded exception method gap in the current API-surface diff.
- Core/Query: added Datazen-style `Query` accessors (`getSQL()`, `getParams()`, `getTypes()`) and `Configuration` type-comment toggle accessors (`getDisableTypeComments()`, `setDisableTypeComments()`) plus mirrored coverage in `src/__tests__/query.test.ts` and `src/__tests__/configuration.test.ts`.
- Platforms/Tooling: made the API diff audit inheritance-aware for TypeScript classes (effective method surface = declared + inherited), which removes subclass override noise in platform parity reports, and added method-rename alias handling for `initializeDoctrineTypeMappings` -> `initializeDatazenTypeMappings`.
- Platforms: closed the remaining platform method-surface gaps in the current audit by adding best-effort vendor helper methods/stubs in `SQLServerPlatform`, `PostgreSQLPlatform`, `OraclePlatform`, and `DB2Platform`, plus `PostgreSQL120Platform::getDefaultColumnValueSQLSnippet()` and explicit `NotSupported::new()` visibility; added/extended platform parity tests in `src/__tests__/platforms/platform-parity.test.ts`.
- Query/Driver: restored a small core API-surface parity batch by adding `SelectQuery`/`UnionQuery` getters, `ForUpdate::getConflictResolutionMode()`, `FetchUtils::fetchAllAssociative()` plus explicit public static helpers, query exception factory aliases (`NonUniqueAlias::new`, `UnknownAlias::new`) and explicit public visibilities on query expression/join helpers; added coverage in `src/__tests__/query/query-api-surface-parity.test.ts` and `src/__tests__/driver/fetch-utils.test.ts`.
- Tooling: updated `scripts/doctrine-api-surface-diff.mjs` visibility matching so a wider TS `public` method satisfies a Doctrine `protected` method during missing-method checks (for example `Connection.connect()` vs Doctrine's protected `connect()`), preventing false positives without changing runtime API visibility.
- Schema: closed the remaining real schema API-surface parity gaps found by the audit (vendor `selectTableNames()` protected overrides for DB2/Oracle/SQL Server, `SchemaConfig::toTableConfiguration()`, `Sequence::getNameParser()`, `View::getNameParser()`, and `UnquotedIdentifierFolding::foldUnquotedIdentifier()` enum alias) and extended parity coverage in `src/__tests__/schema/schema-api-surface-parity.test.ts`.
- Tooling: improved `scripts/doctrine-api-surface-diff.mjs` TypeScript parsing for enum-namespace exported functions and multiline interface method signatures (plus inline object-type interface parameters), eliminating schema diff false positives and reducing schema audit non-excluded missing methods from 75 to 0 in the current report.
- Schema: restored a large `AbstractSchemaManager` protected/private metadata-pipeline parity layer (best-effort wrappers/stubs for `select*`, `fetch*`, `_getPortable*`, current-schema/name normalization helpers, and internal schema-provider/table-introspection helper methods) and added smoke coverage for protected defaults in `src/__tests__/schema/schema-manager.test.ts`; schema audit non-excluded missing methods reduced from 111 to 78.
- Schema: added another small parity cleanup batch (`AbstractOptionallyNamedObject::setName()`, `OptionallyUnqualifiedNamedObjectSet::replace()`, `InvalidForeignKeyConstraintDefinition::formatName()`, and extra schema parity tests including FK enum SQL helper coverage), bringing schema audit non-excluded missing methods down further from 78 to 75.
- Schema: reduced remaining schema API-surface parity gaps with additional low-risk batches across `ComparatorConfig`, `IndexEditor`, `Column`, `Comparator`, `IntrospectingSchemaProvider`, `PrimaryKeyConstraintEditor`, `UniqueConstraint`, `AbstractAsset`, and `AbstractNamedObject`, plus extended parity coverage in `src/__tests__/schema/schema-api-surface-parity.test.ts`; schema audit non-excluded missing methods reduced from 145 to 111 (with `schema/collections/object-set` still appearing as a diff-script false positive even though the interface methods already exist).
- Schema: reduced API-surface parity gaps further by restoring `ColumnEditor` quoted/unquoted and platform-option helper setters (`setQuotedName`, `setUnquotedName`, `setMinimumValue`, `setMaximumValue`, `setEnumType`, `setDefaultConstraintName`) plus low-risk internal/protected parity wrapper methods in `Schema`, `Table`, `Index`, and `ForeignKeyConstraint`; added `ColumnEditor` parity coverage in `src/__tests__/schema/schema-api-surface-parity.test.ts`.
- Schema: restored `Comparator::diffSequence()` and wired sequence alteration detection into `Comparator.compareSchemas()` (including best-effort auto-increment sequence skipping via a private helper), with sequence parity coverage added to `src/__tests__/schema/schema-comparator-editor.test.ts`; schema audit non-excluded missing methods reduced from 188 to 186.
- Schema: restored `OracleSchemaManager` public overrides for `createDatabase()` (best-effort Datazen flow: create user + optional password + `GRANT DBA`) and `dropTable()` (parity override that currently delegates to base drop until Oracle autoincrement cleanup is ported), with integration SQL-capture coverage in `src/__tests__/schema/schema-manager.test.ts`; schema audit non-excluded missing methods reduced from 190 to 188.
- Schema: restored small vendor schema-manager public overrides by adding PostgreSQL/SQL Server `listSchemaNames()` implementations and a best-effort SQL Server `createComparator()` override (returning the current base `Comparator` until a SQLServer-specific comparator is ported), with integration coverage in `src/__tests__/schema/schema-manager.test.ts`; schema audit non-excluded missing methods reduced from 193 to 190.
- Schema: restored a mutating `AbstractSchemaManager` public API parity batch by adding platform-SQL-backed `drop*` / `create*` / `alter*` / `renameTable()` / `migrateSchema()` shims and schema object execution helpers wired through `Connection.executeStatement()`, with integration coverage that captures executed SQL in `src/__tests__/schema/schema-manager.test.ts`; schema audit non-excluded missing methods reduced from 215 to 193.
- Schema: restored a low-risk `AbstractSchemaManager` public API parity batch by adding list/introspection aliases and shims (`listDatabases`, `listSchemaNames`, `listSequences`, `listTableColumns`, `listTableIndexes`, `listTableForeignKeys`, `tablesExist`, many `introspect*` wrappers), plus `createSchemaConfig()` and `createComparator()` with safe default SQL hooks for databases/schemas/sequences; extended integration coverage in `src/__tests__/schema/schema-manager.test.ts` and reduced schema audit non-excluded missing methods from 244 to 215.
- Schema: restored `ForeignKeyConstraintEditor` quoted/unquoted helper setter API (`setQuotedName`, `setUnquotedName`, quoted/unquoted referencing/referenced column setters, quoted/unquoted referenced-table setters) and aligned `setDeferrability()` to emit Datazen-compatible `deferrable`/`deferred` options while preserving the existing `deferrability` option key for compatibility; added coverage in `src/__tests__/schema/schema-api-surface-parity.test.ts` and reduced schema audit non-excluded missing methods from 252 to 244.
- Schema: restored `Schema`, `SchemaDiff`, and `SchemaProvider` public API parity helpers by adding `SchemaDiff` getters/`isEmpty()`, `Schema` helpers (`getName()` override, `renameTable()`, best-effort `toSql()`/`toDropSql()`), and expanding `SchemaProvider` + `IntrospectingSchemaProvider` with Datazen-style table/view/sequence/schema/database introspection methods (delegation with safe fallbacks), plus parity coverage in `src/__tests__/schema/schema-api-surface-parity.test.ts`; schema audit non-excluded missing methods reduced from 287 to 252.
- Schema: restored `Index` and `ForeignKeyConstraint` public API parity helpers by adding Datazen-style `Index` getters/helpers (`getIndexedColumns`, `getType`, `isClustered`, `getPredicate`, `removeFlag`, `overrules`) and `ForeignKeyConstraint` aliases/getters (`getReferencedTableName`, local/foreign unquoted accessors, foreign-table quoting/name helpers, referential-action/match/deferrability getters, `intersectsIndexColumns(Index)` overload), with coverage added to `src/__tests__/schema/schema-api-surface-parity.test.ts`; schema audit non-excluded missing methods reduced from 303 to 287.
- Schema: restored a `Table` public API parity batch (`modifyColumn`, `addPrimaryKeyConstraint`, `dropPrimaryKey`, `getPrimaryKeyConstraint`, `renameIndex`, `columnsAreIndexed`, `dropForeignKey`, unique-constraint accessors/mutators, comment/schema-config setters, `getRenamedColumns`, `renameColumn`) plus `TableEditor.create()` preservation of explicit unique constraints; extended `src/__tests__/schema/schema-api-surface-parity.test.ts` coverage and reduced schema audit non-excluded missing methods from 324 to 303.
- Schema: restored a broad `TableEditor` public API parity batch (`setQuotedName`/`setUnquotedName`, column/index/constraint rename/drop/modify helpers, primary-key add/drop, `setComment`, `setConfiguration`) with best-effort rename propagation into indexes/primary-key/unique/foreign-key definitions, plus extended coverage in `src/__tests__/schema/schema-api-surface-parity.test.ts`; schema audit non-excluded missing methods reduced further from 350 to 324.
- Schema: added a first API-surface parity batch for low-risk schema classes and collections by restoring Doctrine-named methods in `ColumnDiff`, `TableDiff`, `UnqualifiedNamedObjectSet`, `OptionallyUnqualifiedNamedObjectSet`, `Sequence`, `SequenceEditor`, `ViewEditor`, and `UniqueConstraintEditor`, plus new coverage in `src/__tests__/schema/schema-api-surface-parity.test.ts` and a measurable schema audit reduction (non-excluded missing methods down from 411 to 350 in the current best-effort diff script output).
- Tooling: added `scripts/doctrine-api-surface-diff.mjs` for best-effort Datazen/Doctrine vs port API surface diffing with explicit `core` and `schema` scopes, and exclusion filters for out-of-scope PHP magic methods (`__toString`, `__clone`, etc.) plus cache-related methods so they are tracked as excluded instead of reported as parity gaps.
- API parity: compared Datazen/Doctrine vs port method surfaces for `Connection`, `Result`, `QueryBuilder`, and driver-related classes (best-effort) and added missing port-relevant compatibility methods in `src/connection.ts` (`getDatabase`, transaction-isolation getters/setters, iterator helpers, identifier quoting wrappers, nest-savepoint getters/setters, alias methods) and `src/result.ts` (iterator helpers), with parity coverage in `src/__tests__/connection/connection-transaction.test.ts`.
- Platforms: implemented best-effort `AbstractPlatform` create-table DDL generation (`getCreateTableSQL()` / `getCreateTablesSQL()`) using Datazen schema assets (columns/indexes/foreign keys), including foreign-key deferral in multi-table creates, with parity tests in `src/__tests__/platforms/platform-parity.test.ts`.
- Platforms: expanded `src/platforms/abstract-platform.ts` public API surface toward Datazen `AbstractPlatform` parity by adding missing Doctrine-named aliases and DDL/schema helper methods (with default SQL or `NotSupported` stubs where full behavior is not yet ported), plus a reference-backed public-method surface audit test in `src/__tests__/platforms/abstract-platform-method-surface-parity.test.ts`.
- Platforms: restored additional Datazen-style `AbstractPlatform` public APIs for parity (`getBitAndComparisonExpression()`, `getBitOrComparisonExpression()`, `getDropTableSQL()`, `getDropTemporaryTableSQL()`, `getDropIndexSQL()`) with coverage in `src/__tests__/platforms/platform-parity.test.ts`.
- Platforms: added Doctrine-style `NoColumnsSpecifiedForTable` (`src/platforms/exception/no-columns-specified-for-table.ts`) and an `AbstractPlatform` create-table column guard helper (`assertCreateTableHasColumns(...)`) with parity coverage in `src/__tests__/platforms/platform-parity.test.ts`.
- Tests: started incremental source-mirrored test layout enforcement by moving `ExpandArrayParameters` coverage to `src/__tests__/expand-array-parameters.test.ts` (mirrors `src/expand-array-parameters.ts`) and adding an audit guard in `src/__tests__/tools/test-suite-structure.test.ts` for file-by-file migration.
- Tests: expanded `PrimaryReadReplicaConnection` parity coverage to mirror more of Datazen/Doctrine functional expectations, adding explicit `fetchAllAssociative` read-on-replica behavior, `insert()` write pinning/read-after-write on primary, multi-charset replica inheritance checks, and close/reconnect-to-primary assertions in `src/__tests__/connection/primary-read-replica-connection.test.ts`.
- Driver: implemented per-driver in-memory result classes (`src/driver/mysql2/result.ts`, `src/driver/pg/result.ts`, `src/driver/sqlite3/result.ts`, `src/driver/mssql/result.ts`) and switched those driver connections to use them instead of the shared `ArrayResult`, using `FetchUtils` for shared fetch helpers and adding parity tests.
- Driver: added missing Doctrine-style `AbstractOracleDriver` (`src/driver/abstract-oracle-driver.ts`) wired to `OraclePlatform` and the OCI exception converter, with parity coverage in `src/__tests__/driver/abstract-oracle-driver.test.ts`.
- Result: restored Doctrine-style `Result` constructor parity to store the DBAL `Connection`, updated `Connection`/`Statement` call sites to pass it, and wrapped driver-result method exceptions through `connection.convertException(...)` (with result tests covering conversion).
- Porting: added `src/porting.ts` with pragmatic Node helpers for PHP-style ported code (`array_change_key_case`, `is_int`, `is_string`, `is_boolean`/`is_bool`, `key`, `array_key_exists`, `array_fill`, `method_exists`, `array_column`, `assert`, `version_compare`), with `version_compare()` now backed by `semver` plus a small PHP-suffix normalization shim (`dev`/`alpha`/`beta`/`rc`/`pl`), and targeted tests in `src/__tests__/tools/porting.test.ts`.
- Statement: restored Doctrine-style `Statement` construction/parity (`Connection` + driver `Statement` + SQL), removed the non-Doctrine `StatementExecutor` wrapper path, and fixed `Statement.bindValue()` to perform Datazen `Type`/type-name conversion and binding-type resolution before delegating to the driver statement; added statement tests covering type-name/type-instance binding conversion and execution error query-context capture.
- Exception: removed the port-specific `DbalException` base class and switched DBAL exception pass-through detection to an internal marker helper (`src/exception/_util.ts`), updated adapter runtime guard errors to plain `Error`, and aligned Doctrine exception hierarchy/tests (including `SyntaxErrorException`/`TransactionRolledBack` server-exception inheritance and `sql-syntax` test usage -> `SyntaxErrorException`).
- Exception: made `src/exception/driver-exception.ts` support Doctrine-style wrapping (`new DriverException(driverException, query?)`) with query-aware prefixed messages plus `getSQLState()`/`getQuery()`, while preserving the current converter-facing normalized-details constructor during the rewrite.
- Tests: cleaned up remaining failing `logging`, `portability`, `query`, `result`, `tools`, `schema`, `types`, and `package` suites to use direct imports (no removed barrels), Doctrine-shaped driver test doubles (`prepare/query/exec` + `ArrayResult`), and internal binding-style test helpers instead of public `ParameterBindingStyle`; full test suite is green again.
- Exception: implemented Doctrine-style `InvalidColumnType` base exception and nested factory exceptions (`ColumnLengthRequired`, `ColumnPrecisionRequired`, `ColumnScaleRequired`, `ColumnValuesRequired`) with targeted parity tests.
- Tests/connection: rewrote remaining connection-suite driver spies to the Doctrine-shaped driver API (`prepare/query/exec` + driver `Result`) and fixed direct imports (no deleted barrels), including explicit built-in type registration in isolated type-conversion tests.
- Exception: added Doctrine-named exception shims/aliases for parity (`ServerException`, `SyntaxErrorException`, `TransactionRolledBack`, `ConnectionLost`, `LockWaitTimeoutException`, `SavepointsNotSupported`, `DatabaseRequired`, `ReadOnlyException`, `NoActiveTransaction`, `CommitFailedRollbackOnly`, `RetryableException`) with targeted tests.
- Tests: aligned `PrimaryReadReplicaConnection` unit coverage more closely with Doctrine functional cases by adding explicit `keepReplica` transaction/insert stickiness checks and reconnect-to-replica behavior after `close()`.
- Reimplemented `src/connections/primary-read-replica-connection.ts` as a Doctrine-style DBAL connection wrapper (not a driver wrapper): primary/replica connection caches, sticky primary semantics, `keepReplica`, explicit `ensureConnectedToPrimary()` / `ensureConnectedToReplica()`, replica parameter charset inheritance, and primary-forcing on write/transaction/savepoint/prepare operations; also restored `Connection` extension hooks (`performConnect`, protected exception conversion/current-driver setters) and added DBAL savepoint methods for subclass parity.
- Aligned driver-module tests more closely with Doctrine DBAL test structure by splitting middleware abstract wrapper coverage into `src/__tests__/driver/middleware/*`, adding a Doctrine-style `src/__tests__/driver/version-aware-platform-driver.test.ts`, and removing non-Doctrine public `name`/`bindingStyle` assertions from concrete driver tests.
- Rewrote the runtime driver module to Doctrine-shaped APIs (`prepare/query/quote/exec/lastInsertId` + driver `Statement`/`Result`) for `sqlite3`, `mysql2`, `pg`, and `mssql`; removed driver-level `executeQuery`/`executeStatement` and savepoint methods from the public driver connection contract, moved nested savepoints to DBAL `Connection` platform SQL, and switched `src/result.ts`/driver middleware/portability wrappers to real driver results instead of migration DTOs.
- Removed port-specific `DriverQueryResult` / `DriverExecutionResult` from the public `src/driver.ts` API surface and moved them to internal `src/driver/internal-result-types.ts` to keep the exported driver API closer to Doctrine.
- Added missing Doctrine-style `AbstractDB2Driver` base class in `src/driver/abstract-db2-driver.ts` (Db2 platform + IBM DB2 exception converter wiring) with parity tests.
- Added Doctrine-style driver middleware wrapper bases in `src/driver/middleware/*` (`AbstractConnectionMiddleware`, `AbstractDriverMiddleware`, `AbstractResultMiddleware`, `AbstractStatementMiddleware`) with delegation tests and async-aware TS signatures for Node driver contracts.
- Completed Doctrine-style DB2/OCI exception-converter parity for field/table/database-not-found mappings by porting missing DBAL exception classes (`InvalidFieldNameException`, `NonUniqueFieldNameException`, `TableNotFoundException`, `TableExistsException`, `DatabaseDoesNotExist`, `DatabaseObjectNotFoundException`) and wiring Oracle/DB2 SQLCODE/ORA mappings to them with updated converter tests.
- Refactored Node driver adapters (`sqlite3`, `mysql2`, `pg`, `mssql`) to inherit new Doctrine-style abstract driver base classes (`AbstractSQLiteDriver`, `AbstractMySQLDriver`, `AbstractPostgreSQLDriver`, `AbstractSQLServerDriver`), centralizing platform selection and driver API exception-converter wiring while preserving async `connect()` behavior for Node clients.
- Ported Doctrine-style Oracle OCI API exception conversion in `src/driver/api/oci/exception-converter.ts`, including Oracle error-code mappings and best-effort nested `ORA-02091` rollback-cause conversion, with targeted unit tests.
- Ported Doctrine-style IBM DB2 API exception conversion in `src/driver/api/ibmdb2/exception-converter.ts`, including DB2 SQLCODE mappings for syntax, FK, unique, not-null, and connection errors, plus targeted converter tests.
- Ported Doctrine-style Oracle `EasyConnectString` rendering and connection-parameter assembly in `src/driver/abstract-oracle-driver/easy-connect-string.ts`, including `service` deprecation warning behavior and unit coverage for descriptor generation branches.
- Added a larger Doctrine-parity batch with canonical path moves (moved `PrimaryReadReplicaConnection` implementation to `src/connections/primary-read-replica-connection.ts` and removed the top-level compatibility shim), new Doctrine-shaped array-parameter exceptions, driver exception abstractions/factories, SQL Server/SQLite driver middleware parity files, and Doctrine-path API converter aliases (`driver/api/postgre-sql`, `driver/api/sql-server`) while allowing breaking import changes.
- Tightened Doctrine parity auditing with DataZen path normalization rules (`mysql`, `sqlite3`, `sqlite`, `sql-server`, `pgsql`), added a code naming scan for `Doctrine*`/`PHP*` tokens in `src`, corrected placeholder namespace folder names to match those conventions, and split `Query\\ForUpdate\\ConflictResolutionMode` into its own file for one-type-per-file parity.
- Added a repeatable Doctrine parity audit script (`scripts/doctrine-parity-audit.mjs`), generated a structural audit report (`docs/doctrine-parity-audit.json`), and scaffolded missing Doctrine-mapped `src/*` namespace folders with `.gitkeep` placeholders for incremental file-by-file porting.
- Updated the Doctrine parity audit to exclude intentional driver exceptions (PHP-specific Doctrine driver namespaces and Node-specific adapter naming differences such as `mysql2`, `pg`, `sqlite3`, `mssql`) and the Doctrine `cache/*` subsystem (PHP PSR-dependent) from missing-file counts.
- Refined Doctrine parity naming in `src/driver/primary-read-replica/*` by exporting the wrapper as `Connection` from `connection.ts` and using import aliases at call sites instead of a Doctrine-nonexistent `PrimaryReadReplicaDriverConnection` class name.
- Added Doctrine-inspired `PrimaryReadReplicaConnection` support with `DriverManager.getPrimaryReadReplicaConnection(...)`, replica-read/primary-write routing, primary pinning during transactions, and explicit switching helpers.
- Documentation: refreshed parity/status docs to reflect current runtime support (`mysql2`, `mssql`, `pg`, `sqlite3`), platform coverage (MySQL/MariaDB, PostgreSQL, SQLite, SQL Server, Oracle, Db2), partial version-based platform selection, and partial Schema module availability.
- Breaking: made `Driver.getDatabasePlatform(versionProvider)` driver-owned and required, removed `Connection` driver-name platform fallbacks, added Doctrine-style `StaticServerVersionProvider` selection from `serverVersion` / `primary.serverVersion`, introduced semver-based MySQL/MariaDB versioned platform resolution (`MySQL80/84`, `MariaDB1052/1060/1010/110700`), and now throw `InvalidPlatformVersion` for malformed platform version strings.
- Added `pg` and `sqlite3` driver adapters (connections, exception converters, driver-manager registration, and driver barrel exports) with best-effort Doctrine-style PostgreSQL/SQLite platform classes and DSN scheme aliases (`postgres*` -> `pg`, `sqlite` -> `sqlite3`).
- Added package subpath namespace exports so consumers can import grouped APIs from:
  - `@devscast/datazen/driver`
  - `@devscast/datazen/exception`
  - `@devscast/datazen/logging`
  - `@devscast/datazen/platforms`
  - `@devscast/datazen/portability`
  - `@devscast/datazen/query`
  - `@devscast/datazen/schema`
  - `@devscast/datazen/sql`
  - `@devscast/datazen/tools`
  - `@devscast/datazen/types`
- Added namespace barrel entry points for `driver`, `query`, `sql`, and `tools`.
- Updated build configuration to emit multi-entry bundles/types for subpath exports.
- Added coverage test for namespace barrels and `package.json` subpath export declarations.
- Breaking: reduced root `@devscast/datazen` exports to modules backed by files directly under `src/`; grouped APIs now require subpath imports (for example `@devscast/datazen/query`, `@devscast/datazen/types`, `@devscast/datazen/platforms`).
- Documentation: corrected outdated schema/keyword-list status notes and added a living parity matrix guide (`docs/parity-matrix.md`).

# 1.0.2 - Schema Foundation Parity
- Ported a Doctrine-inspired schema foundation under `src/schema/*`:
  - schema assets: `AbstractAsset`, `Identifier`, `Column`, `Index`, `ForeignKeyConstraint`, `Table`, `Sequence`, `View`, `Schema`, `SchemaConfig`
  - schema management: `AbstractSchemaManager`, `MySQLSchemaManager`, `SQLServerSchemaManager`, `OracleSchemaManager`, `DB2SchemaManager`
  - schema-manager factories: `SchemaManagerFactory`, `DefaultSchemaManagerFactory`
- Added best-effort file-by-file Schema namespace parity with Doctrine reference layout:
  - created missing files/sub-namespaces under `src/schema/*` (`collections`, `default-expression`, `exception`, `foreign-key-constraint`, `index`, `introspection`, `metadata`, `name`)
  - added `src/schema/module.ts` as a stable schema-module export surface
  - exposed schema module namespace from top-level API as `SchemaModule`
- Added parity scaffolding and practical implementations for schema diffing/building APIs:
  - `Comparator`, `ComparatorConfig`, `ColumnDiff`, `TableDiff`, `SchemaDiff`
  - editor builders (`ColumnEditor`, `TableEditor`, `IndexEditor`, `ForeignKeyConstraintEditor`, `PrimaryKeyConstraintEditor`, `UniqueConstraintEditor`, `SequenceEditor`, `ViewEditor`)
  - constraint and table-configuration objects (`PrimaryKeyConstraint`, `UniqueConstraint`, `TableConfiguration`)
- Added `Connection.createSchemaManager()` and `Configuration` support for:
  - `schemaManagerFactory` override injection
  - `schemaAssetsFilter` hook (default pass-through)
- Ported Doctrine keyword-list support in `src/platforms/keywords/*`, including MySQL/MariaDB versioned lists and PostgreSQL/SQLite keyword classes.
- Extended platform integration to expose:
  - `getReservedKeywordsList()`
  - dialect-specific schema manager creation via `createSchemaManager(connection)`
- Added coverage tests:
  - `src/__tests__/platforms/keywords.test.ts`
  - `src/__tests__/schema/schema-assets.test.ts`
  - `src/__tests__/schema/schema-manager.test.ts`
  - `src/__tests__/schema/schema-comparator-editor.test.ts`
  - `src/__tests__/schema/schema-file-parity.test.ts`
- Expanded Doctrine parity coverage for schema exceptions in `src/__tests__/schema/schema-exception-parity.test.ts`:
  - added message assertions for previously untested schema exception factories (`IncomparableNames`, `IndexNameInvalid`, `InvalidIdentifier`, `InvalidName`, `InvalidTableName`, `NotImplemented`, `UniqueConstraintDoesNotExist`, `UnsupportedName`)
  - added full `InvalidState` factory message coverage and additional `InvalidTableModification` factory/cause assertions
- Ported additional Doctrine schema internals beyond scaffolding:
  - `src/schema/index/indexed-column.ts` now validates and exposes indexed-column metadata (name + optional positive length)
  - `src/schema/name/*` and `src/schema/name/parser/*` now implement Doctrine-style name value objects, identifier folding, and SQL-like parser behavior
  - `src/schema/metadata/*` row classes now carry Doctrine-style metadata DTO state/getters
  - `src/schema/introspection/metadata-processor/*` now combine metadata rows into schema editors/objects (indexes, PK/FK constraints, sequences, views)
  - added editor compatibility helpers used by metadata processors (`IndexEditor`, `ForeignKeyConstraintEditor`, `PrimaryKeyConstraintEditor`, `SequenceEditor`, `ViewEditor`)
- Added schema parity behavior tests in `src/__tests__/schema/schema-name-introspection-parity.test.ts` covering:
  - name objects/parsers (`GenericName`, `Identifier`, `UnqualifiedName`, `OptionallyQualifiedName`, parser registry)
  - `IndexedColumn`
  - metadata rows and metadata processors

# 1.0.1 - Convenience DML Parity
- Added phase 1 TypeScript-friendly typed row propagation:
  - `Result<TRow>` now carries a default row shape for associative fetch methods.
  - `Connection.executeQuery<T>()` and `Connection.executeQueryObject<T>()` now return `Result<T>`.
  - `Connection` fetch helpers now expose generic return types (`fetchAssociative<T>()`, `fetchAllAssociative<T>()`, etc.).
  - `Statement.executeQuery<T>()` and `QueryBuilder.executeQuery<T>()` now propagate typed row results.
- Added typed-row coverage tests:
  - `src/__tests__/connection/connection-typed-fetch.test.ts`
  - `src/__tests__/result/result.test.ts`
- Implemented Doctrine-style convenience data manipulation methods on `Connection`:
  - `insert(table, data, types?)`
  - `update(table, data, criteria, types?)`
  - `delete(table, criteria, types?)`
- Added Doctrine-parity-focused tests for the new convenience methods in `src/__tests__/connection/connection-data-manipulation.test.ts`, including keyed type maps, null criteria handling, and named-binding compilation paths.
- Aligned convenience DML tests with Doctrine `tests/ConnectionTest.php` parity patterns:
  - empty insert
  - update with different data/criteria columns
  - update with shared column in data and criteria
  - update/delete with `IS NULL` criteria
- Updated documentation to reflect implementation status in `docs/data-retrieval-and-manipulation.md` and `docs/security.md`.

# 1.0.0 - Initial Stable Release
- Added QueryBuilder documentation (`docs/query-builder.md`) aligned with the current ported API and feature set.
- Added portability documentation (`docs/portability.md`) describing middleware flags, configuration, and portability boundaries.
- Added types documentation (`docs/types.md`) covering the DataZen type registry, built-in types, platform type mapping hooks, custom type registration, and current Node-specific behavior.
- Added platform documentation (`docs/platforms.md`) covering platform resolution, implemented platform classes, customization patterns, and current parity limits versus Doctrine's version-specific platform matrix.
- Added security documentation (`docs/security.md`) covering SQL injection boundaries, safe/unsafe APIs, prepared statement usage, parameter typing guidance, and current Datazen-specific limitations.
- Added transaction documentation (`docs/transactions.md`) covering demarcation, transactional closures, nested savepoint behavior, rollback-only state, transaction-related exceptions, and current unimplemented parity points (connection isolation API, retryable markers, lock wait timeout-specific exception).
- Implemented Doctrine-style auto-commit mode controls with `Configuration#setAutoCommit()/getAutoCommit()` and `Connection#setAutoCommit()/isAutoCommit()`, including automatic transaction start on connect and outer transaction restart behavior when auto-commit is disabled.
- Added known vendor issues documentation (`docs/known-vendor-issues.md`) with current Datazen runtime caveats for MySQL and SQL Server plus platform-only notes for Oracle/Db2 and unsupported-vendor scope notes.
- Added database extension guide (`docs/supporting-other-databases.md`) documenting how to implement custom drivers/platforms in Datazen, including registration paths, test expectations, and current schema-module scope limits.
- Added introduction documentation (`docs/introduction.md`) describing Datazen's DBAL scope, runtime-supported vendors, platform coverage, and a quick start example.
- Rewrote `README.md` with current project scope, runtime/platform support matrix, installation and quick-start examples, architecture summary, and a complete documentation index linking all guides in `docs/`.

# 0.0.1 - Initital Beta Release
- Initial release of Datazen TypeScript, a lightweight database abstraction layer inspired by Doctrine DBAL original PHP implementation.
- Provides a unified API for building SQL queries, managing connections, and handling exceptions across multiple database drivers (MySQL, PostgreSQL, SQL Server).
- Added Doctrine-style logging middleware (`src/logging/*`) with connection parameter redaction, query/statement logging, and transaction lifecycle logging via pluggable logger implementations (console-compatible by default).
- Added Doctrine-style portability middleware (`src/portability/*`) with configurable empty-string/null conversion, right-trim normalization, and column-case normalization plus Oracle-specific flag optimization.
- Added Doctrine-inspired `DsnParser` (`src/tools/dsn-parser.ts`) for parsing DSN URLs into connection params with scheme mapping, sqlite path handling, and malformed DSN exceptions.
- Replaced `ParameterCompiler` with Doctrine-style `ExpandArrayParameters` (`src/expand-array-parameters.ts`) and moved query compilation flow into `Connection`, including SQL Server named-binding conversion.
- Added and updated docs for architecture, configuration, and data retrieval/manipulation to reflect current DataZen DBAL parity and current non-implemented scope.
