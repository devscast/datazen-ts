import { describe, it } from "vitest";

describe("Functional/TemporaryTableTest", () => {
  it.skip("Doctrine root-level temporary table functional coverage not ported yet (platform-specific temp table lifecycle semantics batch)", async () => {});
});
