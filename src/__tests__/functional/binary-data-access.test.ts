import { describe, it } from "vitest";

describe("Functional/BinaryDataAccessTest", () => {
  it.skip("Doctrine root-level binary data access functional coverage not ported yet (driver/binary representation differences across Node clients)", async () => {});
});
