import { describe, it } from "vitest";

describe("Functional/DataAccessTest", () => {
  it.skip("Doctrine root-level data access functional coverage not ported yet (fetch-object/class semantics need Node-specific adaptation)", async () => {});
});
