import { describe, it } from "vitest";

describe("Functional/ForeignKeyExceptionTest", () => {
  it.skip("Doctrine root-level foreign key exception functional coverage not ported yet (platform-specific FK exception conversion matrix)", async () => {});
});
