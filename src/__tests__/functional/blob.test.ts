import { describe, it } from "vitest";

describe("Functional/BlobTest", () => {
  it.skip("Doctrine root-level BLOB functional coverage not ported yet (Node buffer/stream semantics differ by driver)", async () => {});
});
