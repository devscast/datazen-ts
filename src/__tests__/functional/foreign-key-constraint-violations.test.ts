import { describe, it } from "vitest";

describe("Functional/ForeignKeyConstraintViolationsTest", () => {
  it.skip("Doctrine root-level FK constraint violations functional coverage not ported yet (cross-platform FK timing/DDL semantics batch)", async () => {});
});
