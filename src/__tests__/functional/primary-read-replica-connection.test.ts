import { describe, it } from "vitest";

describe("Functional/PrimaryReadReplicaConnectionTest", () => {
  it.skip("Doctrine root-level primary/read-replica functional coverage not ported yet (requires replica topology harness beyond current single-target functional setup)", async () => {});
});
