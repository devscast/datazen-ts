import { describe, it } from "vitest";

describe("Functional/ResultMetadataTest", () => {
  it.skip("Doctrine root-level result metadata functional coverage not ported yet (cross-driver metadata shape normalization batch)", async () => {});
});
